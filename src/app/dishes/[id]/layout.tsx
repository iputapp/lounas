export const metadata = {
  title: "Dishes | IMAP",
  description: "This is a dishes page. You can see the detail of dishes at here.",
};

export default function DishesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
