export const metadata = {
  title: "Test BackButton | IMAP",
  description: "PROJECT PIPLUP",
};

export default function TestBackButtonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
