export const metadata = {
  title: "Test navigation | IMAP",
  description: "PROJECT PIPLUP",
};

export default function TestDialogInfoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
