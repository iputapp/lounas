export const metadata = {
  title: "Test DecidedButton | IMAP",
  description: "PROJECT PIPLUP",
};

export default function TestDecidedButtonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
